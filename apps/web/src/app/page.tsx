export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-zinc-50 px-6 text-zinc-950">
      <section className="w-full max-w-xl rounded-2xl border border-zinc-200 bg-white p-8 shadow-sm">
        <p className="text-sm font-semibold uppercase tracking-[0.18em] text-emerald-700">Football Predictor</p>
        <h1 className="mt-3 text-3xl font-semibold tracking-tight">Project foundation is ready.</h1>
        <p className="mt-4 leading-7 text-zinc-600">
          The Web, Worker, shared packages, health checks, tests, and CI baseline are initialized. Product features are added by their owning stories.
        </p>
      </section>
    </main>
  );
}
