import Link from "next/link";
import { BrandMark } from "@/components/brand-mark";

export function AuthShell({ eyebrow, title, description, children, footer }: { eyebrow: string; title: string; description: string; children: React.ReactNode; footer: React.ReactNode }) {
  return <main id="main-content" className="grid min-h-screen lg:grid-cols-[minmax(20rem,.72fr)_1.28fr]"><aside className="hidden border-r rule bg-[var(--ink)] p-12 text-white lg:flex lg:flex-col lg:justify-between"><BrandMark/><blockquote><p className="display text-4xl font-bold leading-tight">“不是猜得准就够了，重要的是每次判断都留下证据。”</p><footer className="mt-6 text-sm text-white/60">比赛日编辑部</footer></blockquote><p className="text-xs leading-5 text-white/55">无充值 · 无提现 · 无兑换 · 仅限 18+</p></aside><section className="flex min-h-screen flex-col"><header className="flex items-center justify-between border-b rule px-4 py-4 sm:px-8"><div className="lg:hidden"><BrandMark/></div><Link href="/" className="ml-auto text-sm font-bold underline-offset-4 hover:underline">返回首页</Link></header><div className="flex flex-1 items-center justify-center px-4 py-10 sm:px-8"><div className="w-full max-w-md"><p className="eyebrow">{eyebrow}</p><h1 className="display mt-3 text-4xl font-bold">{title}</h1><p className="mt-3 leading-7 text-[var(--muted)]">{description}</p><div className="mt-8">{children}</div><div className="mt-8 border-t rule pt-6 text-sm text-[var(--muted)]">{footer}</div></div></div></section></main>;
}
